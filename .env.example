# Airflow (local Docker) — copy to `.env` and adjust.
AIRFLOW_UID=50000
AIRFLOW__CORE__FERNET_KEY=
AIRFLOW__API_AUTH__JWT_SECRET=change_me_in_prod
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# DuckDB (path inside containers; host path is ./data/... via compose mount)
DUCKDB_PATH=/opt/airflow/data/staff.duckdb
EMPLOYEES_LOCAL_PATH=/opt/airflow/data/incoming/employees.csv

# When running dbt on the host (not in Docker), point at the same file, e.g.:
# export DUCKDB_PATH="$PWD/data/staff.duckdb"
